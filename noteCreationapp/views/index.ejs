<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
   <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="main w-full min-h-screen bg-zinc-900">
        <div class="form p-10">
            <form action="/create" method="post">
                <input class="block w-full bg-zinc-800 mt-5 px-5 py-2 rounded-md outline-none text-white"  type="text" placeholder="text goes here" name="title">
                <textarea class="block w-full bg-zinc-800 mt-5 px-5 py-2 rounded-md resize-none outline-none text-white" placeholder="write your task details here" name="details"></textarea>
                <input class="bg-blue-500 cursor-pointer px-6 mt-3 rounded-lg py-3" type="submit" value="Create Task">
            </form>
            <div class="tasks p-5 flex gap-3 flex-wrap">
                <% if(files.length > 0){ %>
                   <% files.forEach(function(val){ %>
                    <div class="task min-w-72 mt-3 rounded-lg  bg-zinc-800 py-3 px-4">
                    <h1 class="text-white text-3xl tracking-tighter"><%= val%></h1>
                    <div class="mt-3 flex justify-between item-center">
                        <a class="text-blue-500 inline-block" href="/file/<%= val %>">read more</a>
                        <a class="text-zinc-600 inline-block" href="/edit/<%= val %>">edit filename</a>
                    </div>
                    
                </div>
               <% }) %> 
               <% }else{ %>
                     <h3 class="text-zinc-600">no task yet.</h3>
               <% } %>   
                </div>
            </div>
        </div>
    </div>
</body>
</html>